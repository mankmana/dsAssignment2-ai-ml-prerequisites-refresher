# AI/ML Prerequisite Refresher

## 1. Introduction to Python

This notebook covers the core Python concepts required as a prerequisite for Data Science, Machine Learning, and AI.

The Colab was executed completely with the code, inputs, and outputs saved. The accompanying video walkthrough explains the important code blocks and the corresponding outputs.

### Links

* **Executed Colab:** [Introduction to Python - Executed](https://colab.research.google.com/drive/1yT4TfuLD54Yr_mWNcgU0u3Yy7fqTlcTF#scrollTo=kliJhGlkA-dM)
* **YouTube Walkthrough:** [Introduction to Python Video](https://youtu.be/IQNh9D0allc)

---

## Topics Covered

### Python Basics

* Python syntax
* Comments and indentation
* Variables
* Dynamic typing
* `type()`
* Integers, floats, and strings

### Arithmetic Operations

* Addition
* Multiplication
* Division
* Floor division
* Modulo
* Exponentiation

### Strings

* String creation
* String concatenation
* String comparison
* `capitalize()`
* `title()`
* `upper()`
* `lower()`
* `split()`
* `replace()`

### Lists

* Creating lists
* Indexing
* Negative indexing
* Slicing
* Updating values
* `append()`
* `pop()`
* `remove()`
* `del`
* `sort()`
* `reverse()`
* `count()`
* List concatenation
* List repetition
* Nested lists
* Iterating through lists

### Dictionaries

* Key-value pairs
* Adding and accessing values
* Membership testing
* `keys()`
* `values()`
* `items()`
* `get()`
* `setdefault()`
* Character-frequency counting

### Other Data Structures

* Tuples
* Sets
* Removing duplicate values using sets

### Operators and Control Flow

* Comparison operators
* Logical operators
* Membership operators
* `if`
* `elif`
* `else`
* Conditional expressions

### Loops

* `for` loops
* `range()`
* `while` loops

### Functions

* Defining functions
* Function parameters
* Return values
* Docstrings
* Functions without return values
* Lambda functions

### Built-in Functions

* `len()`
* `min()`
* `max()`
* `sorted()`

### Python Data Processing Utilities

* `map()`
* `filter()`
* List comprehensions
* `enumerate()`
* `zip()`

---

## Key Takeaway

This notebook provides the Python programming foundation needed before moving into numerical computing, data manipulation, visualization, and machine learning.

The main concepts include working with data structures, controlling program flow, creating reusable functions, and transforming collections of data using Python utilities such as `map()`, `filter()`, list comprehensions, `enumerate()`, and `zip()`.

These concepts will be used throughout later notebooks involving NumPy, Pandas, Matplotlib, and Machine Learning.

